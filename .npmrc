# Local installs use the default registry. The @pangaea scope's publish target
# (GitHub Packages) lives on each package's `publishConfig.registry`, so local
# `npm install` never tries to auth against it. Set NODE_AUTH_TOKEN in CI to publish.
package-lock=true
